#cloud-config
users:
  - name: ${username}
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    hashed_passwd: ${password}
    lock_passwd: false
    ssh_authorized_keys:
      - ${ssh_key}